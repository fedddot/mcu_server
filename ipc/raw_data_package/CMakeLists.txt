add_library(raw_data_package_common INTERFACE)
target_include_directories(raw_data_package_common INTERFACE ${CMAKE_CURRENT_LIST_DIR}/include)

add_library(raw_data_package_reader INTERFACE)
target_include_directories(raw_data_package_reader INTERFACE ${CMAKE_CURRENT_LIST_DIR}/include)
target_link_libraries(raw_data_package_reader INTERFACE ipc_data_reader raw_data_package_common)

add_library(raw_data_package_writer INTERFACE)
target_include_directories(raw_data_package_writer INTERFACE ${CMAKE_CURRENT_LIST_DIR}/include)
target_link_libraries(raw_data_package_writer INTERFACE ipc_data_writer raw_data_package_common)

add_subdirectory(tests EXCLUDE_FROM_ALL)